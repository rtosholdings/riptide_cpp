[build-system]
requires = [
    "setuptools >=42",
    "wheel",
    "cmake >=3.18",
    "ninja; platform_system!='Windows'",
    "numpy >=1.22, <1.24",
    "tbb-devel ==2021.6.*",
    ]
build-backend = "setuptools.build_meta"