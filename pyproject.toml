[build-system]
requires = [
    "setuptools >=42",
    "wheel",
    "cmake >=3.18",
    "ninja; platform_system!='Windows'",
    "numpy >=1.21, <1.23",
    "tbb-devel ==2021.6.*",
    ]
build-backend = "setuptools.build_meta"